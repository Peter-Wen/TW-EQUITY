services:
  - type: web
    name: tw-equity
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -B twse-limit-up-site/app.py
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
