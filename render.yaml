services:
  - type: web
    name: tw-equity
    runtime: python
    rootDir: twse-limit-up-site
    buildCommand: pip install -r requirements.txt
    startCommand: python -B app.py
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
